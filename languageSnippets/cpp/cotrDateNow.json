{
  "C++ Date Now": {
    "prefix": ["foolsDateNow", "foolsNow"],
    "body": ["std::chrono::system_clock::now()"],
    "description": "Gets the current date and time in C++ using std::chrono."
  }
}
