{
  "C++ Date Type": {
    "prefix": ["foolsTypesDate", "typesDate"],
    "body": ["#include <chrono>\n\nstd::chrono::system_clock::time_point"],
    "description": "C++ doesn't have a built-in Date type. Use std::chrono for date and time operations."
  }
}
